// components
import { SidebarFooter } from "@/components/ui/sidebar";
import NavFooterMenu from "@/interface/base/navigation/NavFooterMenu";

const NavFooter = () => {
  return (
    <SidebarFooter>
      <NavFooterMenu />
    </SidebarFooter>
  );
};

export default NavFooter;
